import { LAW_CATEGORIES, USER_ROLE } from "./constant";

export const lawCategoryOptions = LAW_CATEGORIES;
export const roleOptions = USER_ROLE;

export const sortOptions = [
  { label: "Newest", value: "newest" },
  { label: "Oldest", value: "oldest" },
];
